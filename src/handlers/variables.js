/*const config = require('../config');

module.exports = {
    config: {
        dj: '',
        prefix: config.prefix,
        247: {
            enable: false,
            channel: ''
        }
    }
};*/
